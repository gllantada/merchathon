import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#72C65F",
      main: "#72C65F",
      dark: "#72C65F",
      contrastText: "#72C65F",
    },
    secondary: {
      light: "#5f5f5f",
      main: "#5f5f5f",
      dark: "#5f5f5f",
      contrastText: "#000",
    },
  },
});
export default theme;
